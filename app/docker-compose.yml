services:
  craft-explorer:
    build:
      context: ..
      dockerfile: app/Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - app-data:/app/server-data
    restart: unless-stopped

volumes:
  app-data:
