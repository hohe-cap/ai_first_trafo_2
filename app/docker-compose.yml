services:
  craft-explorer:
    build:
      context: ..
      dockerfile: app/Dockerfile
    ports:
      - "${PORT:-3000}:3000"
    environment:
      - ADMIN_SECRET=${ADMIN_SECRET:-}
    volumes:
      - app-data:/app/server-data
    restart: unless-stopped

volumes:
  app-data:
